# Version-consistency scan paths (OSEP-0016, release step 2).
#
# The scan asserts every OpenSandbox-owned version field below equals the
# umbrella version before a release may proceed. Everything not listed
# here (OpenAPI `openapi:` fields, lockfiles, language-runtime pins in
# workflows, third-party constants, examples) is out of scope by design.
#
# Update this list via a normal PR; it does not require an OSEP amendment.

# Charts: version + appVersion (when present) in every Chart.yaml, plus
# the umbrella chart's dependencies[].version and all chart image refs
manifests/charts/base/Chart.yaml
manifests/charts/controller/Chart.yaml
manifests/charts/server/Chart.yaml
manifests/charts/ingress-gateway/Chart.yaml
manifests/charts/node-agent/Chart.yaml
manifests/charts/fast-sandbox/Chart.yaml
manifests/charts/opensandbox/Chart.yaml
manifests/charts/**/values*.yaml          # image refs -> release-${version}

# JS SDKs: top-level package.json version
sdks/sandbox/javascript/package.json
sdks/code-interpreter/javascript/package.json

# Kotlin/JVM multi-project
sdks/sandbox/kotlin/gradle.properties

# .NET: both version fields + dependency range
sdks/Directory.Build.props

# Python: hatch-vcs tag_regex + git_describe_command (umbrella pattern
# only) and inter-OpenSandbox dependency ranges
server/pyproject.toml
cli/pyproject.toml
sdks/sandbox/python/pyproject.toml
sdks/code-interpreter/python/pyproject.toml
sdks/mcp/sandbox/python/pyproject.toml

# Go: version constant (none today; add the file here if one appears)
